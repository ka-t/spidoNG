// generated by spido-pg generator. do not edit by hand.

#pragma once

#include <spido/server.h>
#include <spido_pg/db.h>
#include <spido_pg/endpoint_registry.h>

namespace svc {

// Build the DbConfig from the bake-time config.json. Called once at startup.
spido_pg::DbConfig build_db_cfg();

// Register every resource's EndpointPolicy with the runtime registry. The
// generator emits one policy per resource; runtime hot-path lookups are
// O(1) array indexing by EndpointId.
void register_endpoint_policies(spido_pg::EndpointRegistry& reg);

// Wire the built-in /_spido_pg/* debug & metrics endpoints onto `srv`.
// Cheap JSON snapshots — useful for health checks and forensics.
void register_debug_endpoints(spido::Server& srv, spido_pg::Db& db);

} // namespace svc
