// generated by spido-pg generator. do not edit by hand.
//
// {{ r.path }} → {{ r.table }}  (endpoint_id={{ r.endpoint_id }})
// methods: {{ r.methods | join(', ') }}

#pragma once

#include <spido/server.h>
#include <spido_pg/db.h>

namespace svc::handlers {

// Wire every {{ r.methods | join('/') }} route for the {{ r.table }} resource
// onto `srv`. Reads through `db` (pool, cache, batch writer, registry).
void register_{{ r.table }}_routes(spido::Server& srv, spido_pg::Db& db);

} // namespace svc::handlers
