// generated by spido-pg generator. do not edit by hand.
//
// Auth endpoints: /auth/login (email+password), /auth/refresh (rotation),
// /auth/logout (revoke). Issues JWT access tokens signed with the same
// secret spido's verifier uses, plus opaque random refresh tokens
// stored in PG so logout / revocation can take effect.

#pragma once

#include <spido/server.h>
#include <spido_pg/db.h>

namespace svc::auth {

void register_auth_routes(spido::Server& srv, spido_pg::Db& db);

} // namespace svc::auth
